from typing import type_check_only

'''''
s = "foo"
t = "bar"

1. map (s:t)
2. set (t)
3. loop throught s & t
    4.if not in map
    if t in set return False
    else put in map
    5. in in map but value diff its false
{   f: b
    o: a
}
set (items)
''''''